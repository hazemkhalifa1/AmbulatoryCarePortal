@model CreateDocumentTemplateDto

@{
    ViewData["Title"] = Localizer.T("Page.CreateDocumentTemplate");
    var selectedClinicType = (string?)ViewBag.SelectedClinicType;
    var selectedStandard = (string?)ViewBag.SelectedStandard;
}

@section Styles {
    <link rel="stylesheet" href="~/css/document-templates.css" asp-append-version="true" />
}

<div class="fade-in">
    <div class="template-page-header">
        <div class="template-page-header-left">
            <div class="template-page-header-icon">
                <i class="fas fa-file-alt"></i>
            </div>
            <div>
                <h1>@Localizer.T("Page.CreateDocumentTemplate")</h1>
                <p class="template-page-header-sub">Create a new CBAHI standard document template with metadata and file upload</p>
            </div>
        </div>
        <div class="template-page-header-actions">
            <a asp-action="Index" class="back-link">
                <i class="fas fa-arrow-left"></i>
                @Localizer.T("Common.Back")
            </a>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-9">
            <div class="card">
                <div class="card-body p-4">
                    <form method="post" asp-action="Create" enctype="multipart/form-data" role="form" aria-label="Create template form">
                        @Html.AntiForgeryToken()
                        <input type="hidden" asp-for="ClinicType" id="clinicTypeHidden" />

                        <!-- Standard Identification -->
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-tag me-2"></i>Standard Identification
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label class="form-label">@Localizer.T("Doc.ClinicType") <span class="text-danger">*</span></label>
                                        <select id="clinicTypeSelect" class="form-control">
                                            <option value="">-- @Localizer.T("Doc.SelectClinicType") --</option>
                                            <option value="AMB">AMB CBAHI</option>
                                            <option value="Dental">Dental CBAHI</option>
                                        </select>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="DepartmentCategory" class="form-label">@Localizer.T("Template.DepartmentCategory") <span class="text-danger">*</span></label>
                                        <select asp-for="DepartmentCategory" class="form-control" id="departmentCategorySelect">
                                            <option value="">-- @Localizer.T("Doc.SelectStandard") --</option>
                                        </select>
                                        <span asp-validation-for="DepartmentCategory" class="form-error"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="StandardCode" class="form-label">Standard Code <span class="text-danger">*</span></label>
                                        <input asp-for="StandardCode" class="form-control" placeholder="e.g. LD1, FMS07" required aria-required="true" />
                                        <span class="form-help">Unique identifier for this standard</span>
                                        <span asp-validation-for="StandardCode" class="form-error"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Title Information -->
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-heading me-2"></i>Title Information
                            </div>
                            <div class="row g-3">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="TitleEn" class="form-label">@Localizer.T("Doc.TitleEn") <span class="text-danger">*</span></label>
                                        <input asp-for="TitleEn" class="form-control" placeholder="Document title in English" required aria-required="true" />
                                        <span asp-validation-for="TitleEn" class="form-error"></span>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label asp-for="TitleAr" class="form-label">@Localizer.T("Doc.TitleAr")</label>
                                        <input asp-for="TitleAr" class="form-control" placeholder="عنوان المستند بالعربية" dir="rtl" />
                                        <span asp-validation-for="TitleAr" class="form-error"></span>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Description -->
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-align-left me-2"></i>@Localizer.T("Common.Description")
                            </div>
                            <div class="form-group">
                                <label asp-for="Description" class="form-label">@Localizer.T("Doc.Description") (@Localizer.T("Common.Optional"))</label>
                                <textarea asp-for="Description" class="form-control" rows="3" placeholder="Provide a brief description of this template's purpose..."></textarea>
                                <span asp-validation-for="Description" class="form-error"></span>
                            </div>
                        </div>

                        <!-- Template File -->
                        <div class="form-section">
                            <div class="form-section-title">
                                <i class="fas fa-upload me-2"></i>@Localizer.T("Template.TemplateFile")
                            </div>
                            <div class="upload-zone" id="uploadArea">
                                <div class="upload-initial">
                                    <i class="fas fa-cloud-upload-alt upload-icon"></i>
                                    <h5>@Localizer.T("Template.DragDropUpload")</h5>
                                    <p>Supported format: .docx</p>
                                    <input type="file" name="templateFile" class="d-none" id="templateFile" accept=".docx" aria-label="Template .docx file" />
                                    <button type="button" class="btn btn-outline-primary" id="uploadBtn">
                                        <i class="fas fa-folder-open me-1"></i>@Localizer.T("Button.Upload")
                                    </button>
                                </div>
                                <div class="upload-preview" id="uploadPreview">
                                    <i class="fas fa-file-word file-icon-lg"></i>
                                    <div class="file-details">
                                        <span class="file-name" id="fileName"></span>
                                        <span class="file-size" id="fileSize"></span>
                                    </div>
                                    <button type="button" class="btn btn-sm btn-ghost text-danger" id="removeFile" title="Remove file">
                                        <i class="fas fa-times"></i>
                                    </button>
                                </div>
                            </div>
                            <div class="form-help mt-2">
                                <i class="fas fa-info-circle me-1"></i>
                                Upload a .docx file with placeholders like
                                <code>{{ClinicName}}</code>, <code>{{LicenseNumber}}</code>, etc.
                            </div>
                        </div>

                        <!-- Actions -->
                        <div class="d-flex gap-3 mt-4 pt-3 border-top">
                            <button type="submit" class="btn btn-primary btn-lg flex-fill">
                                <i class="fas fa-save me-2"></i>@Localizer.T("Button.Create")
                            </button>
                            <a asp-action="Index" class="btn btn-secondary btn-lg">
                                <i class="fas fa-times me-2"></i>@Localizer.T("Button.Cancel")
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        var standardsCache = {};

        function loadStandards(clinicType) {
            if (standardsCache[clinicType]) {
                return $.Deferred().resolve(standardsCache[clinicType]).promise();
            }
            return $.get('@Url.Action("GetStandards", "DocumentTemplates", new { area = "SuperAdmin" })', { clinicType: clinicType })
                .then(function(data) {
                    standardsCache[clinicType] = data.standards;
                    return data.standards;
                });
        }

        function populateDepartmentCategory(standards) {
            var $deptSelect = $('#departmentCategorySelect').empty().append('<option value="">-- @Localizer.T("Doc.SelectStandard") --');
            $.each(standards, function(i, s) {
                $deptSelect.append('<option value="' + s + '">' + s + '</option>');
            });
        }

        $('#clinicTypeSelect').on('change', function() {
            var val = $(this).val();
            $('#clinicTypeHidden').val(val);

            var $deptSelect = $('#departmentCategorySelect').empty().append('<option value="">-- @Localizer.T("Doc.SelectStandard") --');

            if (!val) return;

            $deptSelect.html('<option value="">Loading standards...</option>');

            loadStandards(val).done(function(standards) {
                populateDepartmentCategory(standards);
            });
        });

        document.getElementById('uploadBtn').addEventListener('click', function() {
            document.getElementById('templateFile').click();
        });

        document.getElementById('uploadArea').addEventListener('click', function(e) {
            if (e.target !== this && e.target.closest('#uploadBtn')) return;
            if (e.target.closest('#removeFile')) return;
            document.getElementById('templateFile').click();
        });

        document.getElementById('templateFile').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (!file) return;

            document.getElementById('uploadArea').classList.add('has-file');
            document.getElementById('fileName').textContent = file.name;
            document.getElementById('fileSize').textContent = (file.size / 1024).toFixed(1) + ' KB';
        });

        document.getElementById('removeFile').addEventListener('click', function(e) {
            e.stopPropagation();
            document.getElementById('templateFile').value = '';
            document.getElementById('uploadArea').classList.remove('has-file');
        });

        var preselectedType = $('#clinicTypeHidden').val() || '@selectedClinicType';
        if (preselectedType) {
            $('#clinicTypeSelect').val(preselectedType).trigger('change');
        }

        var preselectedStandard = '@selectedStandard';
        if (preselectedStandard) {
            var checkInterval = setInterval(function () {
                var $dept = $('#departmentCategorySelect');
                if ($dept.find('option[value="' + preselectedStandard + '"]').length > 0) {
                    $dept.val(preselectedStandard);
                    clearInterval(checkInterval);
                }
            }, 100);
            setTimeout(function () { clearInterval(checkInterval); }, 5000);
        }

        var uploadArea = document.getElementById('uploadArea');
        uploadArea.addEventListener('dragover', function(e) {
            e.preventDefault();
            this.classList.add('drag-over');
        });
        uploadArea.addEventListener('dragleave', function(e) {
            e.preventDefault();
            this.classList.remove('drag-over');
        });
        uploadArea.addEventListener('drop', function(e) {
            e.preventDefault();
            this.classList.remove('drag-over');
            var files = e.dataTransfer.files;
            if (files.length > 0) {
                document.getElementById('templateFile').files = files;
                var event = new Event('change', { bubbles: true });
                document.getElementById('templateFile').dispatchEvent(event);
            }
        });
    </script>
}
