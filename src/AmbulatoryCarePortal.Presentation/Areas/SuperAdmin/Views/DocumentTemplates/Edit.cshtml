@model UpdateDocumentTemplateDto

@{
    ViewData["Title"] = Localizer.T("Page.EditDocumentTemplate");
    var templateFilePath = (string?)ViewBag.TemplateFilePath;
}

<!-- Hero Banner -->
<div class="template-hero mb-4">
    <div class="template-hero-content">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1 class="template-hero-title"><i class="fas fa-edit me-2"></i>@Localizer.T("Page.EditDocumentTemplate")</h1>
                <p class="template-hero-subtitle mb-0">Update standard code: <strong>@Model.StandardCode</strong></p>
            </div>
            <a asp-action="Index" class="btn btn-light btn-lg">
                <i class="fas fa-arrow-left me-2"></i>@Localizer.T("Common.Back")
            </a>
        </div>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card template-form-card">
            <div class="card-body">
                <form method="post" asp-action="Edit" asp-route-id="@Model.Id" enctype="multipart/form-data" role="form" aria-label="Edit template form">
                    @Html.AntiForgeryToken()
                    <input type="hidden" asp-for="Id" />

                    <!-- Section: Standard Identification -->
                    <div class="form-section">
                        <div class="form-section-header">
                            <i class="fas fa-tag me-2"></i>
                            <span>Standard Identification</span>
                        </div>
                        <div class="form-section-body">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="StandardCode" class="form-label">Standard Code <span class="text-danger">*</span></label>
                                    <input asp-for="StandardCode" class="form-control" required aria-required="true" />
                                    <span asp-validation-for="StandardCode" class="text-danger"></span>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label asp-for="DepartmentCategory" class="form-label">Department Category</label>
                                    <select asp-for="DepartmentCategory" class="form-select">
                                        <option value="">-- Select Category --</option>
                                        <option value="Leadership">Leadership (LD)</option>
                                        <option value="FMS">Facility Management (FMS)</option>
                                        <option value="PFE">Patient & Family Education (PFE)</option>
                                        <option value="LAB">Laboratory (LAB)</option>
                                        <option value="RAD">Radiology (RAD)</option>
                                        <option value="INF">Infection Control (INF)</option>
                                        <option value="HRM">Human Resources (HRM)</option>
                                        <option value="NURS">Nursing (NURS)</option>
                                    </select>
                                    <span asp-validation-for="DepartmentCategory" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Section: Title Information -->
                    <div class="form-section">
                        <div class="form-section-header">
                            <i class="fas fa-heading me-2"></i>
                            <span>Title Information</span>
                        </div>
                        <div class="form-section-body">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label asp-for="TitleEn" class="form-label">Title (English) <span class="text-danger">*</span></label>
                                    <input asp-for="TitleEn" class="form-control" required aria-required="true" />
                                    <span asp-validation-for="TitleEn" class="text-danger"></span>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label asp-for="TitleAr" class="form-label">Title (Arabic)</label>
                                    <input asp-for="TitleAr" class="form-control" dir="rtl" />
                                    <span asp-validation-for="TitleAr" class="text-danger"></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Section: Description -->
                    <div class="form-section">
                        <div class="form-section-header">
                            <i class="fas fa-align-left me-2"></i>
                            <span>Description</span>
                        </div>
                        <div class="form-section-body">
                            <div class="mb-3">
                                <label asp-for="Description" class="form-label">Description</label>
                                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                                <span asp-validation-for="Description" class="text-danger"></span>
                            </div>
                        </div>
                    </div>

                    <!-- Section: Status -->
                    <div class="form-section">
                        <div class="form-section-header">
                            <i class="fas fa-toggle-on me-2"></i>
                            <span>Status</span>
                        </div>
                        <div class="form-section-body">
                            <div class="form-check form-switch">
                                <input asp-for="IsActive" class="form-check-input" role="switch" id="IsActive" />
                                <label asp-for="IsActive" class="form-check-label" for="IsActive">@Localizer.T("Common.Active")</label>
                            </div>
                        </div>
                    </div>

                    <!-- Section: File Upload -->
                    <div class="form-section">
                        <div class="form-section-header">
                            <i class="fas fa-upload me-2"></i>
                            <span>Template File</span>
                        </div>
                        <div class="form-section-body">
                            @if (!string.IsNullOrEmpty(templateFilePath))
                            {
                                <div class="alert alert-info">
                                    <i class="fas fa-file-word me-2"></i>
                                    Current file: <a href="@templateFilePath" target="_blank" class="alert-link">@System.IO.Path.GetFileName(templateFilePath)</a>
                                    <br /><small>Upload a new file to replace the existing one.</small>
                                </div>
                            }
                            <div class="upload-area" id="uploadArea">
                                <div class="upload-area-content">
                                    <i class="fas fa-cloud-upload-alt upload-icon"></i>
                                    <h5>@Localizer.T("Template.DragDropUpload")</h5>
                                    <p class="text-muted mb-3">Supported format: .docx</p>
                                    <input type="file" name="templateFile" class="form-control d-none" id="templateFile" accept=".docx" aria-label="Template .docx file" />
                                    <button type="button" class="btn btn-outline-primary" id="uploadBtn">
                                        <i class="fas fa-folder-open me-1"></i>@Localizer.T("Button.Upload")
                                    </button>
                                </div>
                                <div class="upload-area-preview d-none" id="uploadPreview">
                                    <div class="file-info">
                                        <i class="fas fa-file-word file-icon"></i>
                                        <div class="file-details">
                                            <span class="file-name" id="fileName"></span>
                                            <span class="file-size text-muted" id="fileSize"></span>
                                        </div>
                                        <button type="button" class="btn btn-sm btn-outline-danger" id="removeFile">
                                            <i class="fas fa-times"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                            <small class="text-muted mt-2 d-block">Upload a .docx file with placeholders like <code>{{ClinicName}}</code>, <code>{{LicenseNumber}}</code>, etc.</small>
                        </div>
                    </div>

                    <div class="d-flex gap-2 mt-4">
                        <button type="submit" class="btn btn-primary btn-lg flex-grow-1">
                            <i class="fas fa-save me-2"></i>@Localizer.T("Template.SaveChanges")
                        </button>
                        <a asp-action="Index" class="btn btn-secondary btn-lg">
                            <i class="fas fa-times me-2"></i>@Localizer.T("Button.Cancel")
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.getElementById('uploadBtn').addEventListener('click', function() {
            document.getElementById('templateFile').click();
        });

        document.getElementById('uploadArea').addEventListener('click', function(e) {
            if (e.target.closest('#uploadBtn')) return;
            if (e.target.closest('#removeFile')) return;
            document.getElementById('templateFile').click();
        });

        document.getElementById('templateFile').addEventListener('change', function(e) {
            const file = e.target.files[0];
            if (!file) return;

            document.querySelector('.upload-area-content').classList.add('d-none');
            document.getElementById('uploadPreview').classList.remove('d-none');
            document.getElementById('fileName').textContent = file.name;
            document.getElementById('fileSize').textContent = (file.size / 1024).toFixed(1) + ' KB';
            document.getElementById('uploadArea').classList.add('has-file');
        });

        document.getElementById('removeFile').addEventListener('click', function(e) {
            e.stopPropagation();
            document.getElementById('templateFile').value = '';
            document.querySelector('.upload-area-content').classList.remove('d-none');
            document.getElementById('uploadPreview').classList.add('d-none');
            document.getElementById('uploadArea').classList.remove('has-file');
        });
    </script>
}
