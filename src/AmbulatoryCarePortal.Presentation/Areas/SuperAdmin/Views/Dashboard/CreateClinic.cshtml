@model AmbulatoryCarePortal.Presentation.ViewModels.CreateClinicViewModel

@{
    ViewData["Title"] = "Create Clinic";
}

<div class="row fade-in">
    <div class="col-md-8 offset-md-2">
        <div class="card card-primary">
            <div class="card-header">
                <h3 class="card-title"><i class="fas fa-plus-circle"></i> Clinic Information</h3>
            </div>
            <form method="post" asp-action="CreateClinic" aria-label="Create clinic form">
                <div class="card-body">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Name" class="form-label">Name (English) <span class="required">*</span></label>
                                <input asp-for="Name" class="form-control" required aria-required="true" />
                                <span asp-validation-for="Name" class="invalid-feedback"></span>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="NameAr" class="form-label">Name (Arabic)</label>
                                <input asp-for="NameAr" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="CityEn" class="form-label">City (English)</label>
                                <input asp-for="CityEn" class="form-control" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="CityAr" class="form-label">City (Arabic)</label>
                                <input asp-for="CityAr" class="form-control" />
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label asp-for="ClinicType" class="form-label">Clinic Type <span class="required">*</span></label>
                        <select asp-for="ClinicType" class="form-control" asp-items="Html.GetEnumSelectList<AmbulatoryCarePortal.Domain.Enums.ClinicType>()" required aria-required="true">
                            <option value="">-- Select Type --</option>
                        </select>
                        <span asp-validation-for="ClinicType" class="invalid-feedback"></span>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="LicenseNumber" class="form-label">License Number</label>
                                <input asp-for="LicenseNumber" class="form-control" />
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="LicenseExpiry" class="form-label">License Expiry Date</label>
                                <input asp-for="LicenseExpiry" type="date" class="form-control" />
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card-footer">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Create Clinic</button>
                    <a href="/SuperAdmin/Dashboard/Clinics" class="btn btn-secondary">Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
